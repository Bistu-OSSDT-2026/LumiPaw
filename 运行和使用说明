# LumiPaw 运行和使用说明

## 一、运行环境

本项目支持 Windows 10/11 系统运行。

运行前需要安装：

* Python 3.11 及以上版本
* 项目所需依赖库

安装依赖：

```bash
pip install -r requirements.txt
```

---

## 二、程序运行方式

进入项目目录：

```bash
cd LumiPaw
```

运行主程序：

```bash
python main.py
```

程序启动后会打开 LumiPaw 桌宠应用界面，用户可以通过界面完成任务管理、金币获取以及宠物装饰等操作。

---

## 三、用户使用流程

### 1. 创建任务

用户进入任务管理界面，添加需要完成的任务。

设置任务信息：

* 任务名称
* 预计完成时间

创建完成后，任务会显示在任务列表中。

---

### 2. 开始任务

用户选择目标任务并点击开始任务。

系统会记录任务开始时间，并进入任务执行状态。

---

### 3. 完成任务

任务执行结束后，用户点击结束任务。

系统会根据任务预计时间和实际执行时间判断：

* 达到预计时间：任务正常完成，并获得金币奖励。
* 未达到预计时间（距离截止时间超过10分钟）：任务会结束，用户不会受到金币奖励。
* 未达到预计时间（距离截止时间小于10分钟）：任务结束，用户依旧会收到奖励。

---

### 4. 获取和使用金币

用户完成任务后可以获得金币。

进入商店界面，可以浏览并购买宠物装饰。

购买成功后，装饰会应用到桌宠，使用户可以自定义宠物外观。

---

### 5. 管理桌宠

程序运行后，桌宠会显示在桌面上。

用户可以查看宠物状态，并通过购买的装饰物调整宠物外观。

---

## 四、注意事项

1. 请确保运行前已正确安装 Python 和项目依赖。
2. 请保留项目中的图片资源文件，否则可能导致桌宠或装饰无法正常显示。
3. 不建议直接修改程序配置文件，避免影响软件正常运行。
4. 如果程序无法启动，请检查 Python 版本以及依赖库是否安装成功。
